package Tree;

import BasicClasses.NodeTree;

/**
 * Created by divya on 3/4/17.
 */

/*Let there  be node X :
        It is to be removed , if it is lying on n paths and all paths length is<k then delete that node :
        if we take every node , then we will have to know all the paths in which it is coming .
        to keep track of all paths :
        this is implementation of post order
        left right root :*/
public class RemoveNodesOnRootToLeaf {

    public void removeNodeOnRootToLeaf(NodeTree root){

        if(root == null)
            return;

    }
}
